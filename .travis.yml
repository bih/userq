language: ruby
rvm:
  - "1.9.3"
  - "2.0.0"
  - jruby-19mode

gemfile:
  - Gemfile
  - test/Gemfile.test

services:
  - sqlite3

env:
  - RAILS_ENV=test

script:
  - rails generate userq:install
  - rake test